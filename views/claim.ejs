<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Claim Faucet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container {
            max-width: 600px;
            margin-top: 50px;
        }
        .alert {
            margin-bottom: 20px;
        }
        .form-group label {
            font-weight: bold;
        }
        button {
            width: 100%;
        }
        .back-link {
            display: block;
            margin-top: 20px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">Claim Your Free BTC</h1>

        <% if (message) { %>
            <div class="alert alert-info">
                <%= message %>
            </div>
        <% } %>

        <form action="/claim" method="POST">
            <div class="form-group mb-3">
                <label for="address">Your Wallet Address</label>
                <input type="text" name="address" id="address" class="form-control" required>
            </div>

            <div class="mb-3">
                <div class="g-recaptcha" data-sitekey="<%= process.env.CAPTCHA_SITE_KEY %>"></div>
            </div>

            <button type="submit" class="btn btn-primary">Claim</button>
        </form>

        <a href="/dashboard" class="back-link text-decoration-none">Back to Dashboard</a>
    </div>

    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
